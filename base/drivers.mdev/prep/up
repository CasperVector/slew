#!/bin/rc -e

for (i in 1 2) {
	find /sys -name modalias -type f |
	xargs sort -u | xargs modprobe -b -a >[2] /dev/null || true
}
if ([ -e /sys/module/fb -a ! -e /sys/module/fbcon ]) {
	modprobe -q -b fbcon || true
}

