#!/bin/rc -e

exec >[2=1]
. /etc/slew/lib/srv.rc

logu=s6log
logd=/var/log/`{cdr $self}
args=(n6 s5000000 !gzip t T $logd)
if ([ -f ./data/s6-log.rc ]) . ./data/s6-log.rc

[ -d $logd ] || install -d -o $logu -g adm -m 0750 $logd
exec s6-setuidgid $logu emptyenv -p s6-log $args

